services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: system-revamp
    ports:
      - "3000:3000"
      - "8000:8000"
      - "8001:8001"
      - "8002:8002"
    restart: unless-stopped
